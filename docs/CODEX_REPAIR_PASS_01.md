# Codex repair pass 01 — M33.1-R1

**Status:** CONTINUE — OFFLINE REPAIR ONLY, effective when Issue #83 governance is merged into main and current authority agrees.

**Gate:** M33.1 / Issue #69. **Implementation PR:** #79. **Branch:** `agent/m33-1-native-product-reconstruction`. **Audited starting head:** `686486b0cfd6e1f062a3074b8d1319a0e84549b4`. **Baseline main:** `801aad49f4c5e5fac4626fe18576717d71d19580`.

## Outcome

Make existing project opening and explicit manufacturing-classification decisions trustworthy on the preserved PR. Keep the existing CAD engine, STEP import, native workspace, geometry/persistence infrastructure and all valid prior M33.1 repairs. The assigned findings are **F05, F06 and F11** in [the full audit](FXD_FULL_AUDIT_2026-09-07.md).

## Cost boundary

Use ChatGPT Codex Remote under the user's ChatGPT agentic allowance. **Development API requests: 0. Paid repository dispatchers: 0. Authorized product-runtime live requests: 0.** No API credentials, provider opt-ins, live acceptance execution, paid coding/review route or automatic retry. Inject synthetic providers for execution tests. Profile E remains unspent. This pass does not select a runtime model or claim Astra fixture competence.

## Preflight and current-main synchronization

1. Read current main `AGENTS.md`, `docs/CONTROL_STATE.json`, `CURRENT.md`, `docs/OPERATOR_PROTOCOL.md`, this order, Issue #69 and PR #79's latest head/findings/checks. Verify canonical repo `DumpsterFireWorks/fxd-fixture-design` (GitHub repository ID 1299678045; former `kool1160` URL redirects). If remote identity is different, stop.
2. Confirm revision 4, state REPAIR, pass M33.1-R1, current issue/PR agreement and zero live authorization. If a newer authority changes the assignment, obey it. No duplicate product lane.
3. **Explicit narrow synchronization authorization:** non-destructively merge current main into the existing PR branch before product edits. This is permitted branch integration, not merging PR #79 into main. Preserve product changes and take current-main governance/cost protections for conflicting control files; preserve the branch's M33.1 runtime self-check in CI. Do not force-push, discard unknown work, rewrite history, restore a paid workflow, or invent new authority. If any conflict requires a product/authority choice beyond this rule, stop with exact files/reason.
4. Inspect the resulting diff and run governance/API firewall checks. Protected governance may be synchronized from main verbatim; Codex may not independently edit its scope, authority, budgets or next action.

The standing prompt's conflict stop remains applicable except for this explicitly authorized reconciliation of known stale branch copies. The old held branch alone cannot authorize implementation.

## F06 — complete legacy migration first

Reproduce using an actual v5 project generated by baseline main, with saved proposal and geometry annotations. Use the audit probe or a checked-in synthetic fixture with documented generation provenance. A current-format project relabeled v5 is insufficient.

Repair `project.py` migration and directly dependent proposal handling so source/project data opens without treating partially rewritten derived evidence as current. Define one coherent policy for proposal identity, citations/context, revisions, decisions and approvals. Preserve original history as historical evidence; mark derived evidence stale when it cannot safely carry forward. Do not silently delete the proposal, rehash it into apparent old approval, weaken identity validation, or overwrite the original file during acceptance.

Regress: genuine v5 with/without proposal; annotations and existing edits/build plan where supported; migrated save/reload; unchanged source bytes; prior approvals invalidated when their evidence changes; unknown/ambiguous references and tampered identity rejected. Preserve all earlier true legacy face/component/body migration tests.

## F05 — preserve explicit answers through the real flow

Reproduce a holed plate: reconstruction is unknown, an explicit plate/sheet answer resolves it, execution rebuilds without that answer and blocks before the injected provider.

Repair the existing project/reconstruction/execution contracts and focused native UI entry point. Persist engineer decisions bound to source, component and relevant engineering context. Display the unresolved component/question; accept an explicit supported answer; consume that answer during execution and after save/reload. Do not make the automatic classifier guess plate/tube/formed roles to hide ambiguity.

Regress: holed plate, tube and formed examples remain unknown until explicitly resolved; answered case reaches the synthetic provider once; unanswered case remains blocked; changing source/context or deleting an answer invalidates affected evidence; no stale answer transfers to another component. UI evidence must exercise question/answer → execution → save/reopen, not only banner strings or a low-level constructor.

## F11 — branch and acceptance integration

Retain current-main zero-cost workflow guards and standing prompt. Reconcile obsolete governance assertions by taking current main's tests; do not make a failing check green by deleting the cost checks. Repair the live harness's exact repository identity preflight for the canonical owner with explicit accepted identity/alias verification. Test the preflight in isolation with mocked metadata/process boundaries. **Do not run the live harness or read credentials.** Unrelated lookalike repositories must still fail.

## Expected changed boundaries

- `fxd_geometry/project.py`, `product_reconstruction.py`, `ai_execution.py`.
- `fxd_geometry/ai_fixture_engineer.py` only if required for complete legacy proposal history/staleness.
- `fxd_qt_app.py` and public exports only as required for the focused classification-answer flow.
- Corresponding project, proposal, reconstruction, execution and Qt tests; a synthetic legacy fixture/provenance record if needed.
- `scripts/m33_1_live_acceptance.py` preflight and isolated offline tests only.
- Current-main integration and narrow evidence updates. No independent governance-policy changes.

## Evidence and stop

Run focused regressions, `bash scripts/ci.sh`, pinned OCP proof, M33.1 self-check and `git diff --check`. Preserve secret scans and API firewall tests. Record total test count, skips, platform and exact head accurately. Never turn every audit observation into an expected-failure test; this pass asserts the assigned fixes and leaves later defects explicitly queued.

Obtain native Windows PySide6/VTK evidence for the changed interaction if that environment is available. Otherwise provide the Linux/synthetic evidence actually obtained and explicitly mark Windows acceptance outstanding. Missing Windows access does not justify claiming success or discarding the completed offline repair. No installer replacement or private installed-project modification is required.

Push only to the existing branch and update PR #79 with findings addressed, reproduction/regression evidence, exact SHA and outstanding acceptance. Stop:

```text
AWAITING_REVIEW
Gate: M33.1-R1 / Issue #69
PR: #79
Head: <full SHA>
CI: green | failing | running
Work: <specific repaired behavior>
Blocker: <remaining acceptance, or none for the offline pass>
```

This is completion of the bounded offline repair pass, **not** completion of M33.1. Review-Control must inspect the exact head. Profile E, current-head Windows evidence and all original acceptance requirements remain required before product PR acceptance/merge. No M33.2, R2–R6, final fixture synthesis, AI repair request, core rewrite, deployment or production approval is authorized here.
